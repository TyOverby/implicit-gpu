<svg xmlns="http://www.w3.org/2000/svg">
    <path fill-rule="evenodd" style="fill: rgb(0, 0, 0); stroke: none; stroke-width: none;" d="M2.00,2.00 L2.00,12.00 L12.00,12.00 L12.00,2.00 L11.00,2.00 L10.00,2.00 L2.00,2.00 "/>

<script>
let svgs = document.children[0];
let paths = Array.from(svgs.children).filter(function(c) {
    var orig = c.style.stroke;
    c.onmouseover = function() {
        c.style.stroke = "rgb(0, 0, 255)";
    };
    c.onmouseout = function () {
        c.style.stroke = orig;
    };
    c.onclick = function () {
        svgs.removeChild();
    }
});

for (let path of paths) {
    console.log(path);
}
</script></svg>